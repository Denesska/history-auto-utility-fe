/** Default currency for any amount entered in the app. */
export const DEFAULT_CURRENCY = 'RON';

/**
 * Currencies offered in the price pickers, RON first. A searchable "other
 * currencies" option (full ISO list) is planned as a follow-up.
 */
export const COMMON_CURRENCIES: readonly string[] = ['RON', 'EUR', 'USD', 'MDL'];
